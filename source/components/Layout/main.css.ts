import { style } from "@vanilla-extract/css";
import {
  accentColor,
  backgroundColor,
  primaryColor,
  textColor,
} from "../../base/vars.css";

export const mainCss = style({
  backgroundColor: backgroundColor,
  color: textColor,
});
